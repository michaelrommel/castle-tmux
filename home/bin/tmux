#! /bin/bash

if [[ -x "${HOME}/software/tmux/bin/tmux" ]]; then
	TMUX=${HOME}/software/tmux/bin/tmux
elif [[ -x "/usr/local/bin/tmux" ]]; then
	TMUX=/usr/local/bin/tmux
elif [[ -x "/usr/bin/tmux" ]]; then
	TMUX=/usr/bin/tmux
else
	TMUX=/bin/false
fi

#shellcheck disable=2086,2048
"${TMUX}" $*
